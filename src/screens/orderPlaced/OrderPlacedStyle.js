import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: "#fff",
    },
    title: {
        fontSize: 20,
        textAlign: "center",
        marginTop: 50,
        color: "tomato",
    },
    titleDetail: {
        fontSize: 15,
        marginLeft: 20,
        marginTop: 30,
    },
});

export default styles;
